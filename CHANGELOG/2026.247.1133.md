# 2026.247.1133 — feat: register the crabbox skill family (crabbox wave, Phase 4)

## Summary

Registers the `crabbox` skill family in the `marketplace:` entity's
`families:` map (pod-charly-hooks owns that entity). The crabbox wave repos
declare skill entities in the `crabbox` family (`layer-crabbox` tool skill,
`pod-crabbox` coordinator skill, `plugin-crabbox` check-verb skill);
`charly marketplace generate` refuses unknown families until the map declares
it. Also migrates the root schema to 2026.240.1943 (fresh-binary floor).

## How tested

```
$ charly migrate → migrated to schema 2026.240.1943
$ charly box validate → OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors
```

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | marketplace/layer skills loaded before authoring. |
| R1 — RCA every anomaly | Schema-floor failure RCA'd (fresh-binary floor) + fixed via the sanctioned `charly migrate`; validate 0/0. |
| R2 — finish the whole cutover | Family registration + the migrate land in ONE PR. |
| R3 — no duplication | One family entry mirroring the openclaw pattern; no re-implementation of the marketplace machinery. |
| R4 — no workarounds | Sanctioned migrate path, not a hand edit. |
| R5 — delete legacy | N/A. |
| R6 — git safety | No force-push; branch-per-change PR. |
| R7 — prove behaviour | The families map validates; corpus regeneration consumes it at Phase 4 (marketplace PR). |
| R8 — preserve emitted artifacts | Entity validated (0/0) as the generation input. |
| R9 — binary equals source | Fresh worktree-built bin/charly. |
| R10 — fresh disposable proof | N/A — config/schema entity change (no runtime path). |
| RDD/ADE/SDD | Entity + description authored with the change; no spec changes (N/A SDD). |
| Concurrency | N/A. |
| Disposable-only gate | N/A — no targets created/destroyed. |
| Hooks doctrine | N/A standalone repo; merge gate = org pr-validator. |

## Change classification

- **Class:** marketplace-config (skill family registration).
- **Verification gate:** `charly box validate` (CI + fresh binary), 0/0.
- **Attribution:** fully tested and validated — the changed path (the marketplace `families:` map in charly.yml) was executed live at its boundary (`charly box validate` 0/0 with the fresh binary, the same gate the CI runs); corpus emission is verified in the marketplace PR. Footer matches.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*