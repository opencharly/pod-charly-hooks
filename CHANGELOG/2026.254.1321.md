# 2026.254.1321 — brand: wonky tagline in the marketplace description source

## Summary

Rebrands the owning candy description — the SOURCE the marketplace corpus generator projects into the plugin manifests — to the wonky tagline, with the repo's CI gate fixed IN-TREE (no deferred framing):

1. `charly.yml` `marketplace.marketplace.description`: the brand as a grammatically sound appositive — `Claude Code, Codex, and Kimi plugins for OpenCharly — The wonky DevOps tool for you and your agents: runtime commands, schema authoring, contributor internals, and deployable image families.` (field/quoting/indent unchanged).
2. `.github/workflows/deploy.yml` charly pin `v2026.238.1242` → `v2026.254.0903` (the CI binary era could not read the repo's config schema; the pin is the first half of the in-tree fix).
3. Config migration `2026.240.1943` → `2026.249.2125` (the newest charly's required schema) — the migration that makes `charly box validate` pass with the modern pin. Minimal one-line stamp change; the description survived verbatim at L903.

## How tested

From the pushed tree (branch feat/brand-wonky @ 96b587a, charly built at the pinned tag v2026.254.0903):

```
$ charly version                      -> v2026.254.0903
$ charly migrate                      -> rc 0: migrated to schema 2026.249.2125
$ charly box validate                 -> rc 0: OK — checked 1 candy, 4 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```

- Description verified verbatim post-migration (`…— The wonky DevOps tool for you and your agents: runtime commands, …`); the repo's CI (`candy` workflow) is green on the head.
- `git grep -i 'open infrastructure'` on the final tree = 0 occurrences.
- Diffstat: 2 files, +3/−3 (deploy.yml pin + charly.yml version stamp + description).

The consuming landing (named batch): a marketplace refs pin bump to this merge's tag + corpus regeneration — wonky manifests by generation — then the docs corpus sync.

## Rulebook compliance

- R1 — the manifest source located and fixed at the source; the CI schema mismatch fixed IN-TREE (migration + pin), not parked.
- R3 — one canonical source field; regenerated manifests mirror it.
- R6 — no force-push/amend; single clean commit.

## Change classification

- **Change class:** documentation (description source) + CI pin + config migration (in-tree gate fix).
- **Verification gate:** `charly box validate` rc 0 (pasted) + repo CI green on the head.
- **Attribution tier:** analysed on a live system — the description, migration, and validate ran live with retained output; the corpus projection is the named consuming landing.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*